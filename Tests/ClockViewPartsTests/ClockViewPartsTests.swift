import XCTest
@testable import ClockViewParts

final class ClockViewPartsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ClockViewParts().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
